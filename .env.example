# Public env vars (NEXT_PUBLIC_*) can live here as documentation.
# Secrets belong in the Vercel project settings — never commit them.
#
# Desk API: leave NEXT_PUBLIC_AVA_API unset so the browser uses same-origin
# /api/* (proxied to AVA_ORIGIN_URL / origin.avaivy.cloud).
# AVA_ORIGIN_URL=https://origin.avaivy.cloud
